const ResourceServer = require('./server');
const ResourceClient = require('./client');
const { CollectionAction, InstanceAction } = require('./action');

module.exports = {
  ResourceServer,
  ResourceClient,
  CollectionAction,
  InstanceAction,
};
